

The tour step for "Lançamentos" (line 28) already mentions Telegram briefly ("inclusive pelo Telegram"), but the user wants to make it more prominent — emphasizing that **all** transactions can be done via a Telegram chat.

**Plan:**

Update the `description` for the "Lançamentos" step in `DemoTour.tsx` to highlight the Telegram chat capability more prominently. Something like:

> "Registre todas as suas receitas e despesas em segundos — direto por um chat no Telegram! Organize por categorias, projetos e formas de pagamento sem precisar abrir o sistema."

This is a single-line text change in `src/components/demo/DemoTour.tsx`, line 28.

